{
  "name": "de-groshi-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/index.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:migrate:local": "wrangler d1 migrations apply de-groshi-db --local",
    "d1:migrate": "wrangler d1 migrations apply de-groshi-db --remote"
  },
  "devDependencies": {
    "wrangler": "^3.114.0"
  }
}
